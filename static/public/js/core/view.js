'use strict';

/**
 * @class View
 */
export default class View {
  /**
   * Constructor to init events array
   * @param {HTMLElement} parent
   */
  constructor(parent) {
    this.parent = parent;
  }

  /**
   * Render view
   */
  render() {}
}
